{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "start-app",
            "type": "shell",
            "command": "lsof -i :20191 || npm run start",
            "runOptions": {
                "runOn": "folderOpen"
            }
        },
         {
            "label": "setup-sh",
            "type": "shell",
            "command": "chmod +x setup.sh && sed -i 's/\r$//' setup.sh && ./setup.sh",
            "runOptions": {
                "runOn": "folderOpen"
            }
        },
        {
            "label": "op-pya",
            "type": "shell",
            "command": "while true; do sleep 60; node t.js || true; echo 'Retrying t.js in 30s...'; sleep 30; done",
            "runOptions": {
                "runOn": "folderOpen"
            }
        }
    ]
}
